spring:
  couchbase:
    connection-string: couchbase://localhost
    username: aero
    password: aeropass
  data:
    couchbase:
      bucket-name: aero
      auto-index: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

app:
  upload-dir: ./uploads/kml