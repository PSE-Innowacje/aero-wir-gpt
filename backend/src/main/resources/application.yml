spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/aero
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

app:
  upload-dir: ./uploads/kml
